<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>YT Viral Tag</title>

<style>
@media (min-width:768px){
 body::before{
  content:"📱 Mobile only website";
  position:fixed;inset:0;
  background:#000;color:#fff;
  display:flex;align-items:center;justify-content:center;
  z-index:9999;font-size:22px;
 }
}
body{
 margin:0;background:#0b0b0b;color:#fff;
 font-family:system-ui;
}
.page{display:none;padding:20px}
.active{display:block}
.card{
 background:#151515;
 padding:18px;
 border-radius:14px;
 margin:15px 0;
}
h1{font-size:26px}
p{color:#aaa;font-size:14px}
.btn{
 width:100%;padding:14px;
 border:none;border-radius:10px;
 background:#1a73e8;color:#fff;
 margin-top:10px;font-size:16px;
}
.btn.green{background:#00c853}
.btn.gray{background:#333}
input{
 width:100%;padding:12px;
 border-radius:8px;border:none;
 margin-top:10px;
}
.output{
 background:#0f0f0f;
 padding:12px;border-radius:8px;
 margin-top:10px;font-size:13px;
 word-break:break-word;
}
.lock{
 opacity:.4;pointer-events:none;
}
</style>
</head>

<body>

<!-- HOME -->
<div id="home" class="page active">
 <h1>YT Viral Tag</h1>
 <p>YouTube growth tools</p>

 <div class="card">
  <button class="btn" onclick="login()">Login</button>
  <button class="btn gray" onclick="openTools()">Use Tag Generator</button>
  <p style="margin-top:10px">Login unlocks premium tools</p>
 </div>
</div>

<!-- TOOLS -->
<div id="tools" class="page">
 <h1>Tools</h1>

 <!-- FREE TAG GENERATOR -->
 <div class="card">
  <h3>🎯 Viral Video Tags (FREE)</h3>
  <input id="tagInput" placeholder="Video keyword">
  <button class="btn" onclick="genTags()">Generate</button>
  <div id="tagOut" class="output"></div>
  <button class="btn green" onclick="copy('tagOut')">Copy</button>
 </div>

 <!-- PREMIUM -->
 <div id="premium">

  <div class="card lock" id="titleCard">
   <h3>🚀 Title + Hashtag (Login)</h3>
   <input id="titleInput" placeholder="Video topic">
   <button class="btn">Generate</button>
  </div>

  <div class="card lock" id="descCard">
   <h3>📝 Description + Hashtag (Login)</h3>
   <input id="descInput" placeholder="Video topic">
   <button class="btn">Generate</button>
  </div>

 </div>
</div>

<script>
const isLogin=()=>localStorage.getItem("login")==="yes";

function login(){
 localStorage.setItem("login","yes");
 alert("Login successful");
}

function openTools(){
 home.classList.remove("active");
 tools.classList.add("active");
 if(isLogin()) unlockPremium();
}

function unlockPremium(){
 document.getElementById("titleCard").classList.remove("lock");
 document.getElementById("descCard").classList.remove("lock");

 titleCard.innerHTML=`
 <h3>🚀 Title + Hashtag</h3>
 <input id="titleInput" placeholder="Video topic">
 <button class="btn" onclick="genTitle()">Generate</button>
 <div id="titleOut" class="output"></div>
 <button class="btn green" onclick="copy('titleOut')">Copy</button>`;

 descCard.innerHTML=`
 <h3>📝 Description + Hashtag</h3>
 <input id="descInput" placeholder="Video topic">
 <button class="btn" onclick="genDesc()">Generate</button>
 <div id="descOut" class="output"></div>
 <button class="btn green" onclick="copy('descOut')">Copy</button>`;
}

function genTags(){
 let k=tagInput.value;
 tagOut.textContent=
 `${k}, ${k} viral, ${k} shorts, ${k} youtube, trending video, viral tags, youtube seo`;
}

function genTitle(){
 let k=titleInput.value;
 titleOut.textContent=
 `${k} 🔥 | Full Guide #${k.replace(/\s/g,'')} #viral #youtube`;
}

function genDesc(){
 let k=descInput.value;
 descOut.textContent=
 `This video is about ${k}. Watch till end.\n\n#${k.replace(/\s/g,'')} #viral #youtube`;
}

function copy(id){
 navigator.clipboard.writeText(document.getElementById(id).textContent);
 alert("Copied");
}
</script>

</body>
</html>
